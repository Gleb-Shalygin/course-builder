/**
 * Карта иконок из `resources/icons`: ключ — имя иконки без расширения
 * (например, `logo` или `nav/chevron-right`), значение — содержимое svg-файла.
 */
export type SvgIconsMap = Record<string, string>;
